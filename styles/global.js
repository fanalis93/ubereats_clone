import { StyleSheet } from "react-native";

export const gbstyles = StyleSheet.create({
  container: {
    flex: 1,
    // justifyContent: "center",
    // padding: 8,
    // marginTop: 20,
    backgroundColor: "#eee",
  },
  searchView: {
    // marginTop: 15,
    padding: 10,
    // backgroundColor: "red",
    flexDirection: "row",
  },
});
